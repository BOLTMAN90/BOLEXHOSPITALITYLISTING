export interface Experience {
  id: string;
  title: string;
  category: string;
  image: string;
  duration: string;
  groupSize: string;
  price: number;
  currency: string;
  featured?: boolean;
}
