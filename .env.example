# BOLEXMAN environment variables
# Copy this file to .env.local and fill in values

# Public site URL — used for metadata and absolute links
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Future integrations (not used in MVP)
# OPENAI_API_KEY=

# Note: The interactive map uses Leaflet + free OpenStreetMap/CARTO tiles.
# No map API key is required.
# Newsletter subscriptions are sent directly to belrender000@gmail.com.
